# Rust
/target/
**/*.rs.bk

# Binary workspace: Cargo.lock IS committed (reproducible builds) — do NOT ignore it.

# Pinned contracts checkout for the cross-stack e2e (provided as a sibling or submodule)
/farmore-contracts/

# Secrets / operational state — never commit
.env
*.env
!.env.example
*.key
*.pem
*keystore*
*node-state*.json
farmore-node-state.json

# OS
.DS_Store
